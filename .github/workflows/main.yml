name: ModalView Test

on: [push]

jobs:
  test:
    name: Test
    runs-on: macOS-10.14
    steps:
    - name: Checkout
      uses: actions/checkout@master
    - name: Run tests
      run: swift test
